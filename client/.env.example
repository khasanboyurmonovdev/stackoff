# Cloudflare Turnstile site key for the invisible vote gate.
# Leave unset/empty to disable Turnstile entirely (local dev — votes submit with
# no token, matching the server which skips verification when TURNSTILE_SECRET is
# unset). Set the paired secret as TURNSTILE_SECRET on the server.
VITE_TURNSTILE_SITE_KEY=
