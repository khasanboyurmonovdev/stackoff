node_modules/
.env
dist/
build/
server/audio/
pipeline/out/
__pycache__/

# Machine-specific CA bundle (not project code, never commit)
pipeline/certs/

# Piper voice models (~60MB each, regenerable downloads, not source)
pipeline/voices/

# Raw human recording originals (local-only source; standardized WAVs are committed)
pipeline/human/raw/
